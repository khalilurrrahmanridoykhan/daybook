<!-- What does this PR do and why? Link the roadmap phase if relevant. -->

## Summary

## Checklist

- [ ] `pnpm check` passes (lint + typecheck + test)
- [ ] New logic in `src/lib/services/*` has unit tests
- [ ] DB changes include a Prisma migration
- [ ] Every user-scoped query filters by `userId`
- [ ] No secrets added to tracked files
